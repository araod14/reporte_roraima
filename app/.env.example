# En producción la PWA se sirve desde el mismo dominio que el backend (vía Caddy),
# así que normalmente NO hace falta definir esto (las llamadas van a /api).
#
# Solo defínelo si sirves la PWA en un dominio DISTINTO al del backend:
# VITE_API_BASE_URL=https://reportes.midominio.com
